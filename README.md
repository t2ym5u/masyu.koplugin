# masyu.koplugin

A Masyu puzzle plugin for [KOReader](https://github.com/koreader/koreader).

## Screenshot

*(Screenshot to be added.)*

## Rules

Draw a single closed loop passing through all pearl circles. **White pearl**: the loop goes straight through and must turn in at least one adjacent cell. **Black pearl**: the loop turns 90° here and must go straight through both adjacent cells. The loop cannot branch or cross itself.

## Features

- **Multiple grid sizes**
- **Three difficulty levels** — Easy, Medium, Hard
- **Loop validation** — checks for crossings and branchings
- **Auto-save** — puzzle state saved and restored on next launch

## Installation

1. Download `masyu.koplugin.zip` from the [latest release](../../releases/latest).
2. Extract into the `plugins/` folder of your KOReader data directory.
3. Restart KOReader.
4. Open the menu → **Tools** → **Masyu**.

## Controls

| Action | How |
|--------|-----|
| Toggle loop segment | Tap a grid edge |
| Check progress | Tap **Check** |
| New puzzle | Tap **New** |
| Show rules | Tap **Rules** |

## Known issues

The win-check currently cannot fire: the generated solution is a full-coverage
loop touching every cell, but the win condition checks that each marked cell
has exactly two marked neighbours — a check that a full-coverage loop always
fails on interior cells. In practice, no puzzle generated by the current
version can be marked as solved. A fix requires either switching to sparse
(non-full-coverage) loop generation or tracking drawn edges instead of
per-cell marks. Tracked upstream; not yet fixed.

## License

GPL-3.0 — see [LICENSE](LICENSE).
